const iconMenu = document.querySelector(".menu__list-btn"), menuBody = document.querySelector(".menu__list"); null !== iconMenu && iconMenu.addEventListener("click", (function (e) { document.body.classList.toggle("_lock"), iconMenu.classList.toggle("_active"), menuBody.classList.toggle("_active") })); const anchors = document.querySelectorAll('a[href*="#"]'); for (let e of anchors) e.addEventListener("click", (function (o) { o.preventDefault(); const t = e.getAttribute("href").substr(1); document.getElementById(t).scrollIntoView({ behavior: "smooth", block: "start" }), document.body.classList.remove("_lock"), iconMenu.classList.remove("_active"), menuBody.classList.remove("_active") })); function scrollTopTop() { window.scrollTo({ top: 0, behavior: "smooth" }) } window.addEventListener("scroll", (function () { document.querySelector(".up").classList.toggle("active", window.scrollY > 500) }));
